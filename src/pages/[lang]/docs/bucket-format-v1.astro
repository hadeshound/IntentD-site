---
// Prefixed route for every language except the default one. The body is the
// same screen the English route renders; it reads the language off the URL.
import Screen from '@/screens/docs/bucket-format-v1.astro';
import { prefixedLangPaths } from '@/i18n/routes';

export const getStaticPaths = prefixedLangPaths;
---

<Screen />
